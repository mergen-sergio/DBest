reference=pointer, no computation, direct link
scan=sequential read, iterator, streaming
filter=row-by-row scan, predicate evaluation
projection=column pruning, in-place rewrite
selectColumns=column pruning, in-place rewrite
limit=early termination, counter
duplicateRemoval=linear scan, pairwise comparison
hashDuplicateRemoval=hash table, hashing, bucket
rename=metadata rewrite, no data movement
explode=row multiplication, flatten
autoInc=sequential counter, stateful
group=linear scan, naive grouping
hashGroup=hash table, hashing, bucket
aggregation=accumulator, single pass
sort=comparison sort, in-memory

and=boolean composition, short-circuit
or=boolean composition, short-circuit
xor=boolean composition
condition=expression evaluation, leaf node
if=boolean evaluation, branching

join=nested loop, brute force
mergeJoin=sort-merge, sorted, ordered
hashJoin=hash table, hashing, bucket

semiJoin=nested loop, brute force
mergeLeftSemiJoin=sort-merge, sorted, ordered
hashLeftSemiJoin=hash table, hashing, bucket
mergeRightSemiJoin=sort-merge, sorted, ordered
hashRightSemiJoin=hash table, hashing, bucket

antiJoin=nested loop, brute force
mergeLeftAntiJoin=sort-merge, sorted, ordered
hashLeftAntiJoin=hash table, hashing, bucket
mergeRightAntiJoin=sort-merge, sorted, ordered
hashRightAntiJoin=hash table, hashing, bucket

leftOuterJoin=nested loop, brute force
mergeLeftOuterJoin=sort-merge, sorted, ordered
hashLeftOuterJoin=hash table, hashing, bucket
rightOuterJoin=nested loop, brute force
mergeRightOuterJoin=sort-merge, sorted, ordered
hashRightOuterJoin=hash table, hashing, bucket
mergeFullOuterJoin=sort-merge, sorted, ordered
hashFullOuterJoin=hash table, hashing, bucket

cartesianProduct=nested loop, full pairing
hash=hash function, precomputation
memoize=caching, materialized cache
materialization=eager evaluation, buffer

unilateralExistence=nested loop, brute force
bilateralExistence=nested loop, brute force

append=linear scan, naive concatenation
union=linear scan, pairwise comparison
hashUnion=hash table, hashing, bucket
intersection=linear scan, pairwise comparison
hashIntersection=hash table, hashing, bucket
difference=linear scan, pairwise comparison
hashDifference=hash table, hashing, bucket
