reference=relation, source, table
scan=read, load, iterate
filter=filter, where, condition, predicate, restrict
projection=project, columns, fields, select
selectColumns=select, columns, subset, pick
limit=top, first, restrict
duplicateRemoval=distinct, dedupe, unique
hashDuplicateRemoval=distinct, dedupe, unique
rename=alias, label, rename
explode=flatten, unnest, expand, multivalue
autoInc=identifier, sequence, numbering, index
group=group, partition
hashGroup=group, partition
aggregation=sum, count, avg, min, max, group
sort=order, sort, classify

and=boolean, logical and, conjunction
or=boolean, logical or, disjunction
xor=boolean, exclusive or, logical
condition=boolean, predicate, expression
if=branch, conditional, choose

join=inner, combine, match
mergeJoin=inner, combine, match
hashJoin=inner, combine, match

semiJoin=semi join, left, exists, match
mergeLeftSemiJoin=semi join, left, exists, match
hashLeftSemiJoin=semi join, left, exists, match
mergeRightSemiJoin=semi join, right, exists, match
hashRightSemiJoin=semi join, right, exists, match

antiJoin=anti join, left, missing, no match
mergeLeftAntiJoin=anti join, left, missing, no match
hashLeftAntiJoin=anti join, left, missing, no match
mergeRightAntiJoin=anti join, right, missing, no match
hashRightAntiJoin=anti join, right, missing, no match

leftOuterJoin=outer, left, preserve
mergeLeftOuterJoin=outer, left, preserve
hashLeftOuterJoin=outer, left, preserve
rightOuterJoin=outer, right, preserve
mergeRightOuterJoin=outer, right, preserve
hashRightOuterJoin=outer, right, preserve
mergeFullOuterJoin=outer, full, preserve, both sides
hashFullOuterJoin=outer, full, preserve, both sides

cartesianProduct=cross, cartesian, combine
hash=hash, precompute, key
memoize=cache, reuse, memoize
materialization=materialize, buffer, eager

unilateralExistence=exists, membership, one-sided
bilateralExistence=exists, membership, two-sided

append=concatenate, stack, combine
union=set, combine, all
hashUnion=set, combine, all
intersection=set, common, overlap
hashIntersection=set, common, overlap
difference=set, except, subtract
hashDifference=set, except, subtract
